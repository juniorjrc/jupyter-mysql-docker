version: '3.3'
services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'PROJREDES'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'root'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'root'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      # <Port exposed> : < MySQL Port running inside container>
      - '3307:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
      # Where our data will be persisted
    volumes:
      - my-db:/var/lib/mysql

  datascience-notebook:
      image:    jupyter/datascience-notebook
      volumes:
        - /home/jorgejr/Área de Trabalho/Faculdade/6 SEM/3. LAB. REDES 2/PROJETO-FINAL:/home/jovyan/work
        - /home/jorgejr/Área de Trabalho/Faculdade/6 SEM/3. LAB. REDES 2/PROJETO-FINAL:/home/jovyan/work/datasets
        - /home/jorgejr/Área de Trabalho/Faculdade/6 SEM/3. LAB. REDES 2/PROJETO-FINAL:/home/jovyan/work/modules
        - /home/jorgejr/Área de Trabalho/Faculdade/6 SEM/3. LAB. REDES 2/PROJETO-FINAL:/etc/ssl/notebook
      ports:
        - 8891:8888
      container_name:   jupyter_notebook
# Names our volume
volumes:
  my-db: